# makeUpLab

# Error 1: Caused by small typo.
# Changed from "route_tble_id = aws_route_table.web.id" to "route_table_id = aws_route_table.web.id"


# Error 2: Caused by the same small typo as error 1.


# Error 3: Small typo again. subnt_id      changed to      subnet_id


# Error 4: changed    from_prt   = 80    to         from_port   = 80


# Error 5: Corrected the `user_data` file path from `module/scripts/cloud-init.yaml` to `scripts/cloud-init.yaml`.


# Error 6: Subnet reference from `aws_sbnet.web.id` to `aws_subnet.web.id`.
